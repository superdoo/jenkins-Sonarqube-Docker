@Library('my-shared-library') _

pipeline {
    agent any
    stages {
        stage('Example') {
            steps {
                // Call a function from the shared library
                mySharedFunction()
            }
        }
    }
}
